%h1 Books

= link_to 'New', new_book_path

%table
  %tr
    %td
      Title
    %td
      Authors
  - @books.each do |book|
    %tr
      %td
        = image_tag book.cover.thumb.url
      %td
        = link_to book.title, book_path(book.id)
      %td
        = book.authors
